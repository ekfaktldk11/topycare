# AWS Amplify Configuration
# This file shows example environment variables.
# Actual configuration is handled by amplify_outputs.json

# Note: Amplify Gen 2 uses amplify_outputs.json for configuration
# No environment variables are typically needed for local development

# For production deployment, ensure you have:
# - AWS Account configured
# - Amplify CLI installed: npm install -g @aws-amplify/cli
# - Backend deployed: npx ampx sandbox (for sandbox) or npx ampx deploy (for production)

# Application Settings
VITE_APP_NAME=Topycare
VITE_APP_VERSION=1.0.0
